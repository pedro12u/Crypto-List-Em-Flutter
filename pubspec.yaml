name: cripto_moedas_app
description: Um aplicativo Flutter para listar criptomoedas usando a API CoinMarketCap.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  http: ^1.2.1 
  
  provider: ^6.1.2

  intl: ^0.19.0

  cupertino_icons: ^1.0.6
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
